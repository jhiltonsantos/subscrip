---
description: Full quality gate — run lint, typecheck, and tests.
---

Run the complete quality gate:

```bash
pnpm check
```

This executes: `pnpm lint && pnpm typecheck && pnpm test`.

Report any failures with details on what broke.
